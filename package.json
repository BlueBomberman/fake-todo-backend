{
  "name": "fake-todo-backend",
  "version": "1.0.0",
  "description": "mocked api for react-todos",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "server": "json-server --watch db.json --port 5000"
  },
  "author": "Riccardo Pellegrino",
  "license": "ISC",
  "dependencies": {
    "json-server": "^0.17.0"
  }
}
